server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/transaction_db
    username: ccuser
    password: ccpass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    baseline-on-migrate: true

  data:
    redis:
      host: localhost
      port: 6379

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info

aws:
  region: ap-northeast-1
  s3:
    bucket: cc-platform-bucket
    endpoint: http://localhost:4566
    accessKey: test
    secretKey: test

export:
  maxRows: 50000
  pageSize: 1000
